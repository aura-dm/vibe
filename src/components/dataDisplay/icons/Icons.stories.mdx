import { Meta } from "@storybook/addon-docs";
const icons = import.meta.glob("./*.tsx");
const elements = [];
for (const path in icons) {
  icons[path]().then((mod) => {
    elements.push(mod.default);
  });
}

<Meta title="Components/Data Display/Icons" />

# Vibe Icons

<div style={{ display: "flex", gap: "20px", paddingTop: "12px" }}>
  {elements.map((Icon, i) => {
    return (
      <div
        key={i}
        style={{
          alignItems: "center",
          color: "#666666",
          display: "flex",
          flexDirection: "column",
        }}
      >
        <Icon />
        <p>{Icon.name}</p>
      </div>
    );
  })}
</div>
